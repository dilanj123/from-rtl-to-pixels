[project]
name = "from-rtl-to-pixels"
version = "1.0.0"
requires-python = ">=3.11"
dependencies = [
    "numpy==2.5.3",
    "Pillow==12.3.0",
]

[project.optional-dependencies]
test = [
    "pytest==9.1.1",
]

sim = [
    "cocotb~=2.1",
]

[tool.pytest.ini_options]
testpaths = ["tb/tests"]
