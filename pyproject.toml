[project]
name = "from-rtl-to-pixels"
version = "0.0.0"
requires-python = ">=3.11"
dependencies = [
    "numpy",
    "Pillow",
]

[project.optional-dependencies]
test = [
    "pytest",
]

sim = [
    "cocotb~=2.1",
]

[tool.pytest.ini_options]
testpaths = ["tb/tests"]
